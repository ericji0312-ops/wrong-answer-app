# 오답노트 자동분류 & 취약유형 대시보드 웹앱 — 설계 명세서

> 클로드코드(Claude Code)로 구축하기 위한 기획/설계 문서입니다. 이 문서를 클로드코드 세션에 붙여넣고 "이 명세서대로 웹앱을 만들어줘"라고 요청하면 됩니다.

---

## 1. 프로젝트 개요

**목표**: 학생들이 사진/PDF로 보내는 틀린 문제를 업로드하면, AI가 자동으로 단원/유형을 분류해 데이터베이스에 저장하고, 시험기간에 학생별 취약 유형을 대시보드로 확인할 수 있는 웹앱.

**핵심 사용자 플로우**
1. (선생님) 웹앱 로그인 (공유 비밀번호 방식)
2. 학생 선택 → 틀린 문제 사진/PDF 업로드
3. AI가 사진을 분석해 단원/유형을 자동 분류 → 결과를 화면에 표시
4. 선생님이 분류 결과를 확인/수정 후 저장
5. 시험기간에 "학생별 대시보드" 화면에서 유형별 오답 빈도를 확인 (예: 이차함수 활용 유형 5회, 수열의 극한 3회 등)

---

## 2. 기술 스택

| 구성 요소 | 선택 | 비고 |
|---|---|---|
| 프론트엔드 | React (또는 클로드코드가 제안하는 기본 스택) | 이전 학습스케줄 앱과 동일한 방식 유지 |
| 백엔드/서버 | Node.js (API 라우트) | 이미지 업로드 처리 + Gemini API 호출 + Supabase 연동 |
| 이미지 분류 AI | Gemini API (2.5 Flash, 무료 티어) | 분당 60건 / 일 500건 제한 — 학원 규모에서는 충분 |
| 데이터 저장소 | **Supabase** (무료 플랜) | PostgreSQL DB + 이미지 파일 저장(Storage) 모두 무료 플랜 내에서 처리 가능 |
| 로그인 | 공유 비밀번호 방식 | 기존 학습스케줄 앱과 동일한 방식 |
| 배포/호스팅 | Vercel 무료 플랜 (또는 클로드코드가 제안하는 무료 호스팅) | |

**필요한 준비물 (계정/키 발급)**
- Google AI Studio에서 Gemini API 키 발급 (무료)
- Supabase 프로젝트 생성 (무료 플랜) → API URL + anon key 발급
- (배포 시) Vercel 계정

---

## 3. 데이터베이스 스키마 (Supabase / PostgreSQL)

### `students` (학생)
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | |
| name | text | 학생 이름 |
| grade | text | 학년 (중1/중2/중3/고1/고2/고3) |
| level | text | 비기너/베이직/어드밴스드 |
| created_at | timestamp | |

### `wrong_answers` (오답 기록)
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | |
| student_id | uuid (FK → students.id) | |
| image_url | text | Supabase Storage에 저장된 원본 사진/PDF URL |
| unit | text | 단원명 (예: 이차함수) |
| problem_type | text | 세부 유형 (예: 이차함수의 최댓값·최솟값 활용) |
| ai_confidence | text | AI 분류 신뢰도 or 원본 응답 (검증용, 선택) |
| is_verified | boolean | 선생님이 분류 결과를 확인/수정했는지 여부 |
| recorded_at | timestamp | 오답 등록일 |

> 단원/유형 태그 목록은 추후 원장님이 커리큘럼 구조 기준으로 확정해서 전달할 예정 — 확정되면 `unit_tags` 마스터 테이블을 추가하고 `wrong_answers.unit`/`problem_type`을 이 테이블 참조(FK)로 바꾸는 것을 권장 (자유 텍스트보다 분류 일관성이 훨씬 좋아짐).

---

## 4. AI 분류 로직 (Gemini API)

1. 업로드된 이미지/PDF를 Gemini API (`gemini-2.5-flash`)에 전송
2. 프롬프트: "이 수학 문제 사진을 보고 (1) 큰 단원명 (2) 세부 문제 유형을 분류해줘. 반드시 JSON 형식으로만 응답: {\"unit\": \"...\", \"problem_type\": \"...\"}"
3. (태그 목록 확정 후) 프롬프트에 "다음 목록 중에서만 선택해서 분류해줘: [단원/유형 목록]"을 추가해 분류 일관성 강화
4. 응답 JSON을 파싱해 화면에 미리보기로 표시 → 선생님이 확인/수정 → 저장 버튼 클릭 시 `wrong_answers`에 insert

---

## 5. 화면 구성

### 5-1. 오답 등록 화면
- 학생 선택 (드롭다운)
- 사진/PDF 업로드 (드래그 앤 드롭 또는 파일 선택)
- AI 분류 결과 미리보기 (단원/유형 수정 가능한 입력창)
- 저장 버튼

### 5-2. 학생별 취약유형 대시보드
- 학생 선택
- 기간 필터 (예: 최근 1개월 / 전체)
- 단원별 오답 개수 막대그래프
- 유형별 오답 개수 순위 리스트 (많이 틀린 순)
- 오답 목록 (사진 썸네일 + 단원/유형 + 날짜, 클릭 시 원본 확대)

### 5-3. (선택) 전체 학생 취약유형 통계
- 특정 유형에서 여러 학생이 공통으로 취약한지 확인하는 화면 (반 전체 수업 자료 준비에 활용 가능)

---

## 6. 개발 단계 (클로드코드 진행 순서 제안)

1. Supabase 프로젝트 생성 + 스키마(2번 항목) 테이블 생성
2. Gemini API 키 발급 + 분류 프롬프트 테스트 (샘플 사진 몇 장으로 정확도 확인)
3. 웹앱 기본 골격 (로그인 → 학생 목록 → 오답 등록 화면)
4. 업로드 → AI 분류 → 저장 플로우 연결
5. 학생별 대시보드 화면 구현
6. (선택) 태그 목록 확정 후 `unit_tags` 마스터 테이블 반영 + 분류 프롬프트 고도화
7. Vercel 배포

---

## 7. 향후 확장 고려사항 (지금 당장 필요한 건 아님)
- 학생/학부모가 직접 사진을 업로드할 수 있는 별도 링크 (선생님 경유 없이)
- 오답 재풀이 여부 체크 기능 (같은 유형을 다시 틀리는지 추적)
- 시험 기간 자동 알림 (취약 유형 상위 N개를 선생님에게 자동 요약 발송)
